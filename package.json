{
  "name": "finwise-sem-stock-compare",
  "version": "1.0.0",
  "private": true,

  /* change this to your repo’s Pages URL */
  "homepage": "https://lecroyant.github.io/FinWise-SEMStockCompare",

  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "postbuild": "tailwindcss -i ./src/index.css -o ./build/tailwind.css --minify",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },

  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "recharts": "^2.8.0",
    "d3-dsv": "^3.0.1"
  },

  "devDependencies": {
    "autoprefixer": "^10.4.17",
    "gh-pages": "^6.0.0",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.4"
  },

  "browserslist": {
    "production": [">0.2%", "not dead", "not op_mini all"],
    "development": ["last 1 chrome version", "last 1 firefox version", "last 1 safari version"]
  }
}
